name: Deploy chat-surface

on:
  push:
    branches: [main]
    paths:
      - 'apps/chat-surface/**'
      - 'packages/shared-types/**'
      - '.github/workflows/deploy-chat-surface.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Trigger Vercel production deploy hook
        run: |
          HTTP_CODE=$(curl -sS -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_CHAT_SURFACE }}" \
            -o /tmp/vercel-response.json \
            -w "%{http_code}")
          cat /tmp/vercel-response.json
          if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
            echo "Deploy hook failed with HTTP $HTTP_CODE"
            exit 1
          fi
