name: Deploy did-issuer

on:
  push:
    branches: [main]
    paths:
      - 'apps/did-issuer/**'
      - '.github/workflows/deploy-did-issuer.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Fly.io
        run: echo "Fly.io deploy step for apps/did-issuer/ (TBD)"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
