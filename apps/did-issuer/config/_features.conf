# All default features (issuer-service, web, authentication-service, credential-types)
# are loaded automatically as base features. Do NOT list them in enabledFeatures —
# doing so causes "already exists" errors because they get loaded twice.
enabledFeatures = []
disabledFeatures = []
